# Næste 3 Tasks Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implementér ARCH-163 (typede kolonner → UI), ARCH-213 (budgetkalkulator) og ARCH-181/182 (WFS polygon-caching) i rækkefølge.

**Architecture:** ARCH-163 og ARCH-213 modificerer cockpit-paneler og tilføjer én ny komponent. ARCH-181/182 tilføjer en Supabase-kolonne og cache-logik i det eksisterende map-proxy-lag. Ingen nye API-kald eller store-shape-ændringer udover `budget_estimate`-feltet.

**Tech Stack:** React + TanStack Start, Zustand (project-store), Supabase, Recharts 2.x, OpenLayers 10.x (allerede installeret), bun:test (Jest-kompatibel API)

---

## Vigtige fund inden start

- **ARCH-169 er allerede implementeret** — `computePartialUpdate()` kaldes i `ProjektDnaPanel.patch()` (cockpit/index.tsx:161–176). Luk i Linear.
- **ARCH-181/182 UX er allerede implementeret** — `MatrikelMap.tsx` er en fuldt fungerende OpenLayers-komponent med WFS, WMS, drag/roter/reset og hard stop. `MatrikelCanvas` i ANALYSE-tabben er allerede en wrapper der renderer `<MatrikelMap />`. Kun WFS polygon-caching mangler.
- **ARCH-163 `SaveField`** — bevaringsværdi med farvekodning eksisterer allerede i `EjendomPanel.tsx:310–350`. Det der mangler: `grundareal_m2`/`bebygget_areal_m2` i fallback-kæde, `is_fredet`-badge og `budget_estimate` i OekonomiPanel.

---

## Filkort

**ARCH-163:**
- Modify: `src/lib/project-store.ts` — tilføj `budget_estimate: number | null` + setter
- Modify: `src/routes/__root.tsx` — restore `budget_estimate` fra Supabase
- Modify: `src/components/cockpit/EjendomPanel.tsx` — opdater fallback-kæde + `is_fredet` badge
- Modify: `src/components/cockpit/OekonomiPanel.tsx` — brug typede arealfelter + vis `budget_estimate`

**ARCH-213:**
- Create: `src/components/cockpit/BudgetKalkulator.tsx` — pure beregningsfunktioner + UI + Recharts
- Modify: `src/components/cockpit/OekonomiPanel.tsx` — importer og render `<BudgetKalkulator />`

**ARCH-181/182:**
- Create: `supabase/migrations/20260517000000_add_jordstykke_polygon.sql`
- Modify: `src/integrations/cache/client.ts` — tilføj `getCachedJordstykkePolygon`/`setCachedJordstykkePolygon`
- Modify: `src/lib/map-proxy.ts` — udvid `ParcelGeometryRequest` med `adresseid` + cache-logik
- Modify: `src/components/cockpit/MatrikelMap.tsx` — send `adresseid` med til `loadParcelGeometry`

---

## Task 1: ARCH-163 — Typede kolonner → EjendomPanel + OekonomiPanel

**Files:**
- Modify: `src/lib/project-store.ts`
- Modify: `src/routes/__root.tsx`
- Modify: `src/components/cockpit/EjendomPanel.tsx`
- Modify: `src/components/cockpit/OekonomiPanel.tsx`

- [ ] **Step 1: Tilføj `budget_estimate` til project-store**

I `src/lib/project-store.ts`, find sektionen med ARCH-160 typede felter (efter `hard_stop_reason: string | null`):

```typescript
// I ProjectState interface — tilføj efter hard_stop_reason:
budget_estimate: number | null;
```

Find `setBudgetEstimate` i sektionen med setters (find rækkefølge ved de andre setters fra ARCH-160):
```typescript
// I interface: tilføj efter setHardStop:
setBudgetEstimate: (v: number | null) => void;
```

I initial state (find `hard_stop_reason: null,`):
```typescript
// Tilføj efter hard_stop_reason: null,:
budget_estimate: null,
```

I reset-funktionen (find `hard_stop_reason: null,` i reset):
```typescript
// Tilføj efter hard_stop_reason: null,:
budget_estimate: null,
```

I setters-implementeringen (find `setHardStop: (hard_stop, hard_stop_reason) => set(...)`):
```typescript
// Tilføj efter setHardStop:
setBudgetEstimate: (budget_estimate) => set({ budget_estimate }),
```

- [ ] **Step 2: Restore `budget_estimate` i `__root.tsx`**

I `src/routes/__root.tsx`, find blokken der restorer typede kolonner (find `if (project.heritage_save_value != null) setHeritageSaveValue`):

```typescript
// Tilføj i restore-blokken (efter setBudgetEstimate er destructured fra useProject.getState()):
if (project.budget_estimate != null) setBudgetEstimate(project.budget_estimate);
```

Sørg for at `setBudgetEstimate` er destructured fra `useProject.getState()` i samme blok som de andre setters.

- [ ] **Step 3: Skriv test for `budget_estimate` i store**

```typescript
// Tilføj i src/lib/project-store.test.ts
import { describe, it, expect, beforeEach } from "bun:test";
import { useProject } from "./project-store";

describe("project-store budget_estimate", () => {
  beforeEach(() => {
    useProject.getState().reset();
  });

  it("starter som null", () => {
    expect(useProject.getState().budget_estimate).toBeNull();
  });

  it("sættes og læses korrekt", () => {
    useProject.getState().setBudgetEstimate(2_500_000);
    expect(useProject.getState().budget_estimate).toBe(2_500_000);
  });

  it("nulstilles ved reset", () => {
    useProject.getState().setBudgetEstimate(1_000_000);
    useProject.getState().reset();
    expect(useProject.getState().budget_estimate).toBeNull();
  });
});
```

- [ ] **Step 4: Kør test — forvent FAIL**

```bash
bun test src/lib/project-store.test.ts
```

Forventet output: FAIL med "setBudgetEstimate is not a function" eller lignende (feltet er ikke tilføjet endnu).

- [ ] **Step 5: Verificér `bunx tsc --noEmit` fejler korrekt**

```bash
bunx tsc --noEmit 2>&1 | head -20
```

Forventet: TypeScript-fejl på de nye felter (budget_estimate mangler i interface).

- [ ] **Step 6: Implementér store-ændringer (Step 1+2 ovenfor)**

Gennemfør ændringerne i `project-store.ts` og `__root.tsx` som beskrevet i Step 1 og 2.

- [ ] **Step 7: Kør test igen — forvent PASS**

```bash
bun test src/lib/project-store.test.ts
```

Forventet output: PASS (3 tests)

- [ ] **Step 8: Opdater `EjendomPanel.tsx` — fallback-kæde + `is_fredet` badge**

I `src/components/cockpit/EjendomPanel.tsx`:

**Tilføj til useProject() destructuring** (find `const { complianceMetrics, bbrData, ...`):
```typescript
const {
  complianceMetrics,
  bbrData,
  vurderingData,
  complianceFlags,
  address,
  adressePreCheck,
  heritage_save_value,
  grundareal_m2,        // ← tilføj
  bebygget_areal_m2,    // ← tilføj
  is_fredet,            // ← tilføj
} = useProject();
```

**Opdater fallback-kæde** (find linje ~33 `const grundareal = complianceMetrics...`):
```typescript
const grundareal = grundareal_m2 ?? complianceMetrics?.grundareal ?? bbrData?.grundareal ?? k?.grundareal ?? null;
const bebyggetAreal = bebygget_areal_m2 ?? bbr?.bebygget_areal ?? null;
const currentPct =
  complianceMetrics?.currentBebyggelsesprocent ??
  (grundareal && bebyggetAreal ? (bebyggetAreal / grundareal) * 100 : null) ??
  bbrData?.bebyggelsesprocent ??
  k?.bebyggelsesprocent ??
  null;
```

**Tilføj `is_fredet`-badge** — indsæt i return-sektionen lige efter adresse-rækken (`{address && ...}`) og før noegletal-grid:
```tsx
{is_fredet === true && (
  <div className="flex items-center gap-2 rounded-md border border-danger/40 bg-danger/5 px-3 py-2">
    <AlertTriangle size={13} className="text-danger shrink-0" />
    <div>
      <span className="font-mono text-[10px] tracking-[0.15em] text-danger">FREDET BYGNING</span>
      <span className="ml-2 text-xs text-muted-foreground">— kilde: DAI WFS</span>
    </div>
  </div>
)}
```

**Opdater noegletal-array** (linje ~48) — `GRUNDAREAL` bruger nu `grundareal` (allerede korrekt via opdateret kæde):
```typescript
// Ingen ændring i selve noegletal-arrayet — kæden ovenfor håndterer det
```

- [ ] **Step 9: Opdater `OekonomiPanel.tsx` — typede arealfelter + budget_estimate**

I `src/components/cockpit/OekonomiPanel.tsx`:

**Opdater import** (find `const { vurderingData, bbrData, address } = useProject()`):
```typescript
const { vurderingData, bbrData, address, grundareal_m2, bebygget_areal_m2, budget_estimate } = useProject();
```

**Tilføj beregning af bebyggelsesprocent** (indsæt efter useProject-linjen):
```typescript
const grundareal = grundareal_m2 ?? bbrData?.grundareal ?? null;
const bebyggetAreal = bebygget_areal_m2 ?? bbrData?.bebygget_areal ?? null;
const bebyggelsespct =
  grundareal && bebyggetAreal && grundareal > 0
    ? ((bebyggetAreal / grundareal) * 100).toFixed(1)
    : null;
```

**Tilføj budget_estimate-visning** — indsæt nyt kort i return-sektionen (efter EJENDOMSVURDERING-kortet, før closing `</div>`):
```tsx
{budget_estimate != null && (
  <Card>
    <div className="font-mono text-[11px] tracking-[0.15em] text-muted-foreground mb-4">
      PROJEKTBUDGET (ESTIMAT)
    </div>
    <div className="rounded-md border border-border bg-[#111] p-4">
      <div className="flex items-center gap-2 text-muted-foreground mb-1">
        <span className="font-mono text-[10px] tracking-[0.1em]">SAMLET BUDGET</span>
      </div>
      <p className="text-xl font-medium text-foreground">
        {new Intl.NumberFormat("da-DK", {
          style: "currency",
          currency: "DKK",
          maximumFractionDigits: 0,
        }).format(budget_estimate)}
      </p>
    </div>
    {bebyggelsespct != null && (
      <div className="mt-3 text-xs text-muted-foreground">
        Nuværende bebyggelsesprocent: {bebyggelsespct}%
        {grundareal != null && ` (grundareal ${grundareal} m²)`}
      </div>
    )}
  </Card>
)}
```

- [ ] **Step 10: Byg og verificér**

```bash
bunx tsc --noEmit && bun build
```

Forventet: 0 type-fejl, build grøn.

- [ ] **Step 11: Commit ARCH-163**

```bash
git add src/lib/project-store.ts src/routes/__root.tsx src/components/cockpit/EjendomPanel.tsx src/components/cockpit/OekonomiPanel.tsx src/lib/project-store.test.ts
git commit -m "$(cat <<'EOF'
feat(ARCH-163): typede SSOT-kolonner som primær kilde i EjendomPanel + OekonomiPanel

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: ARCH-213 — Samlet budgetkalkulator

**Files:**
- Create: `src/components/cockpit/BudgetKalkulator.tsx`
- Modify: `src/components/cockpit/OekonomiPanel.tsx`

- [ ] **Step 1: Skriv tests for beregningsfunktioner**

```typescript
// Opret: src/components/cockpit/BudgetKalkulator.test.ts
import { describe, it, expect } from "bun:test";
import {
  beregnNedrivning,
  beregnForsyning,
  beregnGeoteknik,
  beregnNybyg,
  beregnBudget,
  type BudgetInput,
} from "./BudgetKalkulator";

describe("beregnNedrivning", () => {
  it("beregner standard sats (800–1200 kr/m²)", () => {
    const r = beregnNedrivning(100, "1990");
    expect(r.min).toBe(80_000);
    expect(r.max).toBe(120_000);
    expect(r.note).toBeUndefined();
  });

  it("tillægger asbest ved byggeår < 1978", () => {
    const r = beregnNedrivning(100, "1975");
    expect(r.min).toBe(100_000); // 100 × 1000
    expect(r.max).toBe(140_000); // 100 × 1400
    expect(r.note).toContain("asbestrisiko");
  });

  it("returnerer nullresultat ved manglende areal", () => {
    const r = beregnNedrivning(null, "1990");
    expect(r.min).toBe(0);
    expect(r.max).toBe(0);
  });
});

describe("beregnForsyning", () => {
  it("ekskluderer gas ved ikke-naturgas opvarmning", () => {
    const r = beregnForsyning(false);
    expect(r.min).toBe(55_000);
    expect(r.max).toBe(110_000);
  });

  it("inkluderer gas ved naturgas", () => {
    const r = beregnForsyning(true);
    expect(r.min).toBe(65_000); // 55k + 10k
    expect(r.max).toBe(125_000); // 110k + 15k
  });
});

describe("beregnGeoteknik", () => {
  it("kategori 1: god grund", () => {
    const r = beregnGeoteknik(1);
    expect(r.min).toBe(0);
    expect(r.max).toBe(50_000);
  });

  it("kategori 3: dårlig grund / pæl", () => {
    const r = beregnGeoteknik(3);
    expect(r.min).toBe(200_000);
    expect(r.max).toBe(500_000);
  });
});

describe("beregnNybyg", () => {
  it("beregner standard sats (22.000 kr/m²)", () => {
    const r = beregnNybyg(100, null, false);
    expect(r.min).toBe(2_200_000);
    expect(r.max).toBe(2_600_000);
  });

  it("tillægger kælder (+5.000 kr/m²)", () => {
    const r = beregnNybyg(100, null, true);
    expect(r.min).toBe(2_700_000); // 100 × 27.000
    expect(r.max).toBe(3_100_000); // 100 × 31.000
  });

  it("returnerer nullresultat ved manglende areal", () => {
    const r = beregnNybyg(null, null, false);
    expect(r.min).toBe(0);
    expect(r.max).toBe(0);
  });
});

describe("beregnBudget", () => {
  it("summer alle kategorier korrekt", () => {
    const input: BudgetInput = {
      bebyggetArealM2: 100,
      byggeaar: "1990",
      oensketArealM2: 150,
      energiklasse: null,
      harKaelder: false,
      geoteknikKategori: 1,
      naturgas: false,
    };
    const r = beregnBudget(input);
    // Nedrivning: 80k–120k
    // Forsyning: 55k–110k
    // Geoteknik: 0–50k
    // Nybyg: 3.300k–3.900k (150 × 22k–26k)
    expect(r.totalMin).toBe(80_000 + 55_000 + 0 + 3_300_000);
    expect(r.totalMax).toBe(120_000 + 110_000 + 50_000 + 3_900_000);
    expect(r.totalTypisk).toBe(Math.round((r.totalMin + r.totalMax) / 2));
  });
});
```

- [ ] **Step 2: Kør tests — forvent FAIL**

```bash
bun test src/components/cockpit/BudgetKalkulator.test.ts
```

Forventet: FAIL — "Cannot find module './BudgetKalkulator'"

- [ ] **Step 3: Opret `BudgetKalkulator.tsx` med beregningsfunktioner og UI**

```typescript
// src/components/cockpit/BudgetKalkulator.tsx
import { useState, useMemo } from "react";
import {
  BarChart,
  Bar,
  XAxis,
  YAxis,
  Tooltip,
  ResponsiveContainer,
  Cell,
} from "recharts";
import { useProject } from "@/lib/project-store";
import { Card } from "@/components/wizard-ui";

// ---------------------------------------------------------------------------
// Typer
// ---------------------------------------------------------------------------

export type GeoteknikKategori = 1 | 2 | 3;

export type BudgetInput = {
  bebyggetArealM2: number | null;
  byggeaar: string | null;
  oensketArealM2: number | null;
  energiklasse: string | null;
  harKaelder: boolean;
  geoteknikKategori: GeoteknikKategori;
  naturgas: boolean;
};

export type BudgetKategori = {
  label: string;
  min: number;
  max: number;
  note?: string;
};

export type BudgetResultat = {
  nedrivning: BudgetKategori;
  forsyning: BudgetKategori;
  geoteknik: BudgetKategori;
  nybyg: BudgetKategori;
  totalMin: number;
  totalMax: number;
  totalTypisk: number;
};

// ---------------------------------------------------------------------------
// Beregningsfunktioner (pure — eksporteres til test)
// ---------------------------------------------------------------------------

export function beregnNedrivning(
  bebyggetArealM2: number | null,
  byggeaar: string | null,
): BudgetKategori {
  if (!bebyggetArealM2) {
    return { label: "Nedrivning", min: 0, max: 0, note: "Intet registreret bebygget areal" };
  }
  const asbestRisiko = parseInt(byggeaar ?? "0") < 1978;
  const minSats = asbestRisiko ? 1_000 : 800;
  const maxSats = asbestRisiko ? 1_400 : 1_200;
  return {
    label: "Nedrivning",
    min: Math.round(bebyggetArealM2 * minSats),
    max: Math.round(bebyggetArealM2 * maxSats),
    note: asbestRisiko ? "Tillæg for asbestrisiko (byggeår < 1978)" : undefined,
  };
}

export function beregnForsyning(naturgas: boolean): BudgetKategori {
  const gasMin = naturgas ? 10_000 : 0;
  const gasMax = naturgas ? 15_000 : 0;
  return {
    label: "Forsyningsafkobling",
    min: 55_000 + gasMin,
    max: 110_000 + gasMax,
  };
}

export function beregnGeoteknik(kategori: GeoteknikKategori): BudgetKategori {
  const ranges: Record<GeoteknikKategori, [number, number]> = {
    1: [0, 50_000],
    2: [50_000, 200_000],
    3: [200_000, 500_000],
  };
  const [min, max] = ranges[kategori];
  const labels: Record<GeoteknikKategori, string> = {
    1: "Kategori 1 — god grund",
    2: "Kategori 2 — variabel",
    3: "Kategori 3 — dårlig / pæl",
  };
  return { label: "Geoteknik", min, max, note: labels[kategori] };
}

export function beregnNybyg(
  arealM2: number | null,
  energiklasse: string | null,
  harKaelder: boolean,
): BudgetKategori {
  if (!arealM2) {
    return { label: "Nybyg", min: 0, max: 0, note: "Intet ønsket areal angivet" };
  }
  const lavenergitillæg =
    energiklasse &&
    (energiklasse.toLowerCase().includes("lavenergi") || energiklasse.startsWith("A"))
      ? 2_000
      : 0;
  const kaeldertillæg = harKaelder ? 5_000 : 0;
  const baseSatsMin = 22_000 + lavenergitillæg + kaeldertillæg;
  const baseSatsMax = baseSatsMin + 4_000;
  return {
    label: "Nybyg",
    min: Math.round(arealM2 * baseSatsMin),
    max: Math.round(arealM2 * baseSatsMax),
  };
}

export function beregnBudget(input: BudgetInput): BudgetResultat {
  const nedrivning = beregnNedrivning(input.bebyggetArealM2, input.byggeaar);
  const forsyning = beregnForsyning(input.naturgas);
  const geoteknik = beregnGeoteknik(input.geoteknikKategori);
  const nybyg = beregnNybyg(input.oensketArealM2, input.energiklasse, input.harKaelder);
  const totalMin = nedrivning.min + forsyning.min + geoteknik.min + nybyg.min;
  const totalMax = nedrivning.max + forsyning.max + geoteknik.max + nybyg.max;
  return {
    nedrivning,
    forsyning,
    geoteknik,
    nybyg,
    totalMin,
    totalMax,
    totalTypisk: Math.round((totalMin + totalMax) / 2),
  };
}

// ---------------------------------------------------------------------------
// Formatering
// ---------------------------------------------------------------------------

function fmtDKK(v: number): string {
  return new Intl.NumberFormat("da-DK", {
    style: "currency",
    currency: "DKK",
    maximumFractionDigits: 0,
  }).format(v);
}

function fmtShort(v: number): string {
  if (v >= 1_000_000) return `${(v / 1_000_000).toFixed(1).replace(".", ",")} mio.`;
  if (v >= 1_000) return `${Math.round(v / 1_000)}k`;
  return String(v);
}

// ---------------------------------------------------------------------------
// Komponent
// ---------------------------------------------------------------------------

const KATEGORI_LABELS: Record<GeoteknikKategori, string> = {
  1: "Kat. 1 — god grund (0–50k)",
  2: "Kat. 2 — variabel (50–200k)",
  3: "Kat. 3 — dårlig/pæl (200k–500k+)",
};

export function BudgetKalkulator() {
  const { bbrData, byggeoenske, vurderingData, bebygget_areal_m2 } = useProject();

  const [geoteknikKat, setGeoteknikKat] = useState<GeoteknikKategori>(1);
  const [arealOverride, setArealOverride] = useState<string>("");

  const bebyggetAreal = bebygget_areal_m2 ?? bbrData?.bebygget_areal ?? null;
  const oensketAreal =
    arealOverride !== "" ? parseInt(arealOverride) : (byggeoenske?.oensketAreal ?? null);
  const naturgas = bbrData?.opvarmningsmiddel?.toLowerCase().includes("naturgas") ?? false;
  const harKaelder = false; // Byggeoenske har ikke kælder-felt endnu

  const input: BudgetInput = {
    bebyggetArealM2: bebyggetAreal,
    byggeaar: bbrData?.byggeaar ?? null,
    oensketArealM2: typeof oensketAreal === "number" && oensketAreal > 0 ? oensketAreal : null,
    energiklasse: byggeoenske?.energiklasse ?? null,
    harKaelder,
    geoteknikKategori: geoteknikKat,
    naturgas,
  };

  const resultat = useMemo(() => beregnBudget(input), [
    input.bebyggetArealM2,
    input.byggeaar,
    input.oensketArealM2,
    input.energiklasse,
    input.harKaelder,
    input.geoteknikKategori,
    input.naturgas,
  ]);

  const chartData = [
    { name: "Nedrivning", min: resultat.nedrivning.min, max: resultat.nedrivning.max, note: resultat.nedrivning.note },
    { name: "Forsyning", min: resultat.forsyning.min, max: resultat.forsyning.max },
    { name: "Geoteknik", min: resultat.geoteknik.min, max: resultat.geoteknik.max, note: resultat.geoteknik.note },
    { name: "Nybyg", min: resultat.nybyg.min, max: resultat.nybyg.max, note: resultat.nybyg.note },
  ];

  const ejendomsvaerdi = vurderingData?.ejendomsvaerdi ?? null;
  const overEjendomsvaerdi =
    ejendomsvaerdi != null && resultat.totalTypisk > ejendomsvaerdi;

  return (
    <Card>
      <div className="font-mono text-[11px] tracking-[0.15em] text-muted-foreground mb-4">
        PROJEKTKALKULATOR
      </div>

      {/* Inputs */}
      <div className="grid grid-cols-1 sm:grid-cols-2 gap-3 mb-4">
        <div>
          <label className="font-mono text-[10px] tracking-[0.1em] text-muted-foreground block mb-1">
            ØNSKET AREAL (m²)
          </label>
          <input
            type="number"
            min={0}
            max={2000}
            value={arealOverride !== "" ? arealOverride : (byggeoenske?.oensketAreal ?? "")}
            onChange={(e) => setArealOverride(e.target.value)}
            placeholder={String(byggeoenske?.oensketAreal ?? "—")}
            className="w-full rounded-md border border-border bg-[#111] px-3 py-1.5 text-sm text-foreground"
          />
        </div>
        <div>
          <label className="font-mono text-[10px] tracking-[0.1em] text-muted-foreground block mb-1">
            GEOTEKNIK
          </label>
          <select
            value={geoteknikKat}
            onChange={(e) => setGeoteknikKat(Number(e.target.value) as GeoteknikKategori)}
            className="w-full rounded-md border border-border bg-[#111] px-3 py-1.5 text-sm text-foreground"
          >
            {([1, 2, 3] as GeoteknikKategori[]).map((k) => (
              <option key={k} value={k}>
                {KATEGORI_LABELS[k]}
              </option>
            ))}
          </select>
          <div className="mt-1 font-mono text-[9px] text-muted-foreground">
            ⚠ Eksempeldata (GEUS-integration ikke aktiv)
          </div>
        </div>
      </div>

      {/* Vandfaldsbar */}
      <ResponsiveContainer width="100%" height={160}>
        <BarChart data={chartData} layout="vertical" margin={{ top: 0, right: 8, left: 0, bottom: 0 }}>
          <XAxis
            type="number"
            tickFormatter={fmtShort}
            tick={{ fill: "hsl(var(--muted-foreground))", fontSize: 10 }}
            axisLine={false}
            tickLine={false}
          />
          <YAxis
            type="category"
            dataKey="name"
            width={72}
            tick={{ fill: "hsl(var(--muted-foreground))", fontSize: 10 }}
            axisLine={false}
            tickLine={false}
          />
          <Tooltip
            formatter={(value: number) => fmtDKK(value)}
            contentStyle={{
              background: "#111",
              border: "1px solid hsl(var(--border))",
              borderRadius: 6,
              fontSize: 11,
            }}
          />
          <Bar dataKey="min" name="Min" stackId="a" fill="transparent" />
          <Bar dataKey="max" name="Max" stackId="a" radius={[0, 4, 4, 0]}>
            {chartData.map((_, i) => (
              <Cell key={i} fill="hsl(var(--accent))" opacity={0.7 + i * 0.075} />
            ))}
          </Bar>
        </BarChart>
      </ResponsiveContainer>

      {/* Total */}
      <div className="mt-4 rounded-md border border-border bg-[#111] p-4">
        <div className="font-mono text-[10px] tracking-[0.1em] text-muted-foreground mb-2">
          TOTAL ESTIMAT
        </div>
        <div className="text-2xl font-medium text-foreground">{fmtDKK(resultat.totalTypisk)}</div>
        <div className="mt-1 text-xs text-muted-foreground">
          {fmtDKK(resultat.totalMin)} – {fmtDKK(resultat.totalMax)}
        </div>
        {ejendomsvaerdi != null && (
          <div className={`mt-2 text-xs ${overEjendomsvaerdi ? "text-warning" : "text-muted-foreground"}`}>
            {overEjendomsvaerdi
              ? `⚠ Projektet estimeres til ${((resultat.totalTypisk / ejendomsvaerdi) * 100).toFixed(0)}% af ejendomsværdien`
              : `Projektet estimeres til ${((resultat.totalTypisk / ejendomsvaerdi) * 100).toFixed(0)}% af ejendomsværdien`}
          </div>
        )}
      </div>

      {/* Bemærkninger */}
      {chartData.some((d) => d.note) && (
        <ul className="mt-3 space-y-1">
          {chartData.filter((d) => d.note).map((d) => (
            <li key={d.name} className="text-xs text-muted-foreground">
              <span className="font-medium">{d.name}:</span> {d.note}
            </li>
          ))}
        </ul>
      )}
    </Card>
  );
}
```

- [ ] **Step 4: Kør tests — forvent PASS**

```bash
bun test src/components/cockpit/BudgetKalkulator.test.ts
```

Forventet: PASS (8 tests)

- [ ] **Step 5: Wire `BudgetKalkulator` ind i `OekonomiPanel.tsx`**

**Tilføj import** øverst i `src/components/cockpit/OekonomiPanel.tsx`:
```typescript
import { BudgetKalkulator } from "@/components/cockpit/BudgetKalkulator";
```

**Tilføj render** i return-sektionen (helt i bunden af `<div className="space-y-6">`-containeren, efter det eksisterende indhold):
```tsx
<BudgetKalkulator />
```

- [ ] **Step 6: Byg og verificér**

```bash
bunx tsc --noEmit && bun build
```

Forventet: 0 type-fejl, build grøn.

- [ ] **Step 7: Kør alle tests**

```bash
bun test
```

Forventet: alle tests grønne — ingen regressions.

- [ ] **Step 8: Commit ARCH-213**

```bash
git add src/components/cockpit/BudgetKalkulator.tsx src/components/cockpit/BudgetKalkulator.test.ts src/components/cockpit/OekonomiPanel.tsx
git commit -m "$(cat <<'EOF'
feat(ARCH-213): budgetkalkulator i OekonomiPanel — nedrivning, forsyning, geoteknik, nybyg

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: ARCH-181/182 — WFS polygon-caching i address_analysis

**Files:**
- Create: `supabase/migrations/20260517000000_add_jordstykke_polygon.sql`
- Modify: `src/integrations/cache/client.ts`
- Modify: `src/lib/map-proxy.ts`
- `src/routes/api.map-tiles.ts` kræver INGEN ændringer (inputvalidatoren henter `adresseid` automatisk via TypeScript-typeinference på `ParcelGeometryRequest`)
- Modify: `src/components/cockpit/MatrikelMap.tsx`

- [ ] **Step 1: Opret migration**

```sql
-- supabase/migrations/20260517000000_add_jordstykke_polygon.sql
-- Cache jordstykke WFS polygon i address_analysis.
-- Delte data på tværs af brugere — ét jordstykke per adresse, ændres sjældent.

ALTER TABLE public.address_analysis
  ADD COLUMN IF NOT EXISTS jordstykke_polygon      JSONB,
  ADD COLUMN IF NOT EXISTS jordstykke_polygon_at   TIMESTAMPTZ;

COMMENT ON COLUMN public.address_analysis.jordstykke_polygon IS
  'GeoJSON FeatureCollection fra Matriklen2 WFS — jordstykke-polygon for adressens matrikel.';
```

- [ ] **Step 2: Anvend migration**

```bash
bunx supabase db push
```

Alternativt i Supabase Studio SQL-editor — kør SQL fra filen.

Forventet: migration applikeres uden fejl.

- [ ] **Step 3: Skriv test for cache-funktioner**

```typescript
// Tilføj i src/integrations/cache/client.test.ts (opret hvis ikke eksisterer)
import { describe, it, expect } from "bun:test";
import { getCachedJordstykkePolygon, setCachedJordstykkePolygon } from "./client";
```

Bemærk: disse funktioner kalder Supabase — test med `IS_MOCK=true`-mønsteret fra eksisterende tests. Se fx `src/integrations/geus/client.test.ts` for mock-mønsteret der bruges i projektet. Brug det samme mønster:

```typescript
// src/integrations/cache/client.test.ts
import { describe, it, expect } from "bun:test";

// Cache-funktioner kræver Supabase-forbindelse.
// Integration test — kræver SUPABASE_URL og SUPABASE_SERVICE_ROLE_KEY i env.
// Kør med: SUPABASE_URL=... SUPABASE_SERVICE_ROLE_KEY=... bun test src/integrations/cache/client.test.ts

describe("getCachedJordstykkePolygon", () => {
  it("returnerer null for ukendt adresseid", async () => {
    // Kræver live Supabase-forbindelse
    if (!process.env["SUPABASE_URL"]) {
      console.log("Springer over — SUPABASE_URL ikke sat");
      return;
    }
    const result = await getCachedJordstykkePolygon("non-existent-id-12345");
    expect(result).toBeNull();
  });
});
```

- [ ] **Step 4: Tilføj cache-funktioner til `src/integrations/cache/client.ts`**

**Tilføj import** øverst (find `import type * as GeoJSON from "geojson"` — tilføj det hvis det ikke er der):
```typescript
import type * as GeoJSON from "geojson";
```

**Tilføj konstant** (find `const TTL = {`):
```typescript
const TTL = {
  lokalplan: DAYS_MS(30),
  servitut: DAYS_MS(7),
  compliance: DAYS_MS(30),
  report: DAYS_MS(30),
  jordstykke: DAYS_MS(90), // ← tilføj: jordstykker ændres sjældent
};
```

**Tilføj funktioner** i bunden af filen (efter `setCachedReport`):
```typescript
// ---------------------------------------------------------------------------
// Jordstykke polygon (WFS cache)
// ---------------------------------------------------------------------------

export async function getCachedJordstykkePolygon(
  addressId: string,
): Promise<GeoJSON.FeatureCollection | null> {
  const row = await getRow(addressId);
  if (!row) return null;
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const at = (row as any).jordstykke_polygon_at as string | null;
  if (!isFresh(at, TTL.jordstykke)) return null;
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  return (row as any).jordstykke_polygon as GeoJSON.FeatureCollection | null;
}

export async function setCachedJordstykkePolygon(
  addressId: string,
  featureCollection: GeoJSON.FeatureCollection,
): Promise<void> {
  await upsert(addressId, {
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    jordstykke_polygon: featureCollection as any,
    jordstykke_polygon_at: new Date().toISOString(),
  });
}
```

Bemærk: `as any`-cast er nødvendig fordi Supabase types-filen ikke kender de nye kolonner. Kør `bunx supabase gen types typescript --local > src/integrations/supabase/types.ts` efter migration for at fjerne dem.

- [ ] **Step 5: Opdater `ParcelGeometryRequest` i `map-proxy.ts`**

Find typen `export type ParcelGeometryRequest` i `src/lib/map-proxy.ts`:

```typescript
export type ParcelGeometryRequest = {
  point: MapPoint;
  adresseid?: string | null;   // ← tilføj
  bufferMeters?: number;
};
```

**Tilføj import** øverst i `map-proxy.ts` (med de eksisterende imports):
```typescript
import {
  getCachedJordstykkePolygon,
  setCachedJordstykkePolygon,
} from "@/integrations/cache/client";
```

**Opdater `fetchParcelGeometryProxy`** — indsæt cache-check og cache-write:

Find `export async function fetchParcelGeometryProxy(` og opdater den fulde funktion:
```typescript
export async function fetchParcelGeometryProxy(
  request: ParcelGeometryRequest,
): Promise<ParcelGeometryResult> {
  const bbox = createBboxAroundPoint(request.point, request.bufferMeters ?? 140);

  // Cache-check: returner polygon fra address_analysis hvis frisk
  if (request.adresseid) {
    try {
      const cached = await getCachedJordstykkePolygon(request.adresseid);
      if (cached) {
        return { bbox25832: bboxToArray(bbox), featureCollection: cached, source: "wfs" };
      }
    } catch {
      // Cache-fejl er ikke blokerende — fortsæt til WFS
    }
  }

  const apiKey = ensureApiKey();
  const url = new URL(MAT_WFS_URL);
  url.searchParams.set("apikey", apiKey);
  url.searchParams.set("service", "WFS");
  url.searchParams.set("version", "2.0.0");
  url.searchParams.set("request", "GetFeature");
  url.searchParams.set("typenames", "mat:Jordstykke_Gaeldende");
  url.searchParams.set("srsname", "urn:ogc:def:crs:EPSG::25832");
  url.searchParams.set("bbox", bboxToWkt(bbox));
  url.searchParams.set("outputFormat", "application/json");

  const res = await fetch(url, {
    headers: {
      Accept: "application/json, application/geo+json;q=0.9, */*;q=0.8",
    },
  });

  if (!res.ok) {
    return { bbox25832: bboxToArray(bbox), featureCollection: null, source: "missing" };
  }

  const contentType = res.headers.get("content-type") ?? "";
  const text = await res.text();
  if (!text.trim()) {
    return { bbox25832: bboxToArray(bbox), featureCollection: null, source: "missing" };
  }

  if (!contentType.includes("json") && !text.trim().startsWith("{")) {
    return { bbox25832: bboxToArray(bbox), featureCollection: null, source: "missing" };
  }

  try {
    const parsed = JSON.parse(text) as GeoJSON.FeatureCollection;
    const result: ParcelGeometryResult = {
      bbox25832: bboxToArray(bbox),
      featureCollection: parsed,
      source: parsed.features.length > 0 ? "wfs" : "missing",
    };

    // Skriv til cache hvis vi har adresseid og fik resultater
    if (request.adresseid && result.source === "wfs" && result.featureCollection) {
      setCachedJordstykkePolygon(request.adresseid, result.featureCollection).catch(() => {
        // Fire-and-forget — cache-fejl stopper ikke svaret
      });
    }

    return result;
  } catch {
    return { bbox25832: bboxToArray(bbox), featureCollection: null, source: "missing" };
  }
}
```

- [ ] **Step 6: Opdater `MatrikelMap.tsx` — send `adresseid` med**

Find i `src/components/cockpit/MatrikelMap.tsx` linjen `const geometry = await loadParcelGeometry({`:

```typescript
const geometry = await loadParcelGeometry({
  data: {
    point: geo,
    adresseid: address?.adresseid ?? null,  // ← tilføj
    bufferMeters: 180,
  },
});
```

- [ ] **Step 7: Byg og verificér**

```bash
bunx tsc --noEmit && bun build
```

Forventet: 0 type-fejl, build grøn.

Bemærk: TypeScript kan klage over de nye kolonner i `address_analysis` da `types.ts` ikke er opdateret endnu. Kast-kommentarerne (`as any`) i cache/client.ts håndterer det.

- [ ] **Step 8: Kør alle tests**

```bash
bun test
```

Forventet: alle tests grønne.

- [ ] **Step 9: Commit ARCH-181/182**

```bash
git add supabase/migrations/20260517000000_add_jordstykke_polygon.sql src/integrations/cache/client.ts src/lib/map-proxy.ts src/components/cockpit/MatrikelMap.tsx
git commit -m "$(cat <<'EOF'
feat(ARCH-181): WFS jordstykke-polygon caches i address_analysis

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
EOF
)"
```

---

## Afsluttende oprydning

- [ ] **Luk ARCH-169 i Linear** — markér som Done (allerede implementeret i `ProjektDnaPanel.patch()`)
- [ ] **Luk ARCH-181/182 i Linear** — markér som Done
- [ ] **Opdater Supabase typer** (valgfrit men anbefalet):

```bash
bunx supabase gen types typescript --local > src/integrations/supabase/types.ts
```

- [ ] **Kør `/sync-docs`** — orchestrator og pre-check er uændrede, men OekonomiPanel + EjendomPanel er ændret → verificér at INTEGRATIONS.md og data-sources.md ikke er stale.
